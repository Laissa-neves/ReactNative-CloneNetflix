import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    margin: 8,
    alignItems: "center",
  },
  image: {
    width: 150,
    height: 200,
    borderRadius: 8,
  },
  title: {
    marginTop: 8,
    color: "#fff",
    fontSize: 16,
  },
});
