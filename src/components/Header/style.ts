import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    padding: 10,
    alignItems: "center",
    backgroundColor: "#000",
  },
  logo: {
    width: 100,
    height: 40,
    resizeMode: "contain",
  },
});
